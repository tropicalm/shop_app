<table class="table table-striped">
	<thead><tr><td>Id</td><td>Nazwa</td><td>Cena</td><td></td><td></td></tr></thead>
	<tbody>
		<% @products.each do |product| %>
			<tr>
				<td><%= product.id %></td>
				<td><%= product.name %></td>
				<td><%= product.price %></td>
				<td><%= link_to "edytuj", edit_admin_product_path(product) %>
				<td><%= link_to "usuń", [:admin, product], :method => :delete, :confirm => "Na pewno?" %>
			</tr>
		<% end %>
	</tbody>
</table>